[project]
name = "evg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "flask-wtf>=1.3.0",
    "gunicorn>=21.0",
    "python-dotenv>=1.2.2",
    "werkzeug>=3.0",
]
